name: 05 - 1 - Event filters activity typees

on:
  pull_request:
    types:
        - opened
        - synchronize
    branches:
        - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running wheneever a pull request is opened or synchronized AND base branch is main"
